# Agent Rules (Critical Data Safety Protocols)

以下のルールは、データの消失や破壊を防ぐために、全てのセッションにおいて**絶対遵守**しなければならない。

## 1. データの削除・破壊に関する禁止事項

### 1-1. 全件削除メソッドの使用禁止
検証スクリプト、一時的な実行コード、または本番コードにおいて、以下のメソッドをテーブル全体に対して実行することを**厳禁**とする。
- `destroy_all`
- `delete_all`
- `truncate`

> **例外**: 明示的な `where` 句でスコープが限定されている場合、または `ActiveRecord::Base.transaction` ブロック内で `raise ActiveRecord::Rollback` を使用して確実にロールバックされることが保証されている場合のみ許可される。

### 1-2. マスタデータの保護
インポート元となるマスタデータ（例: `Wikipage`）、および運用の核となるデータ（例: `ActivityRecord`）に対して、検証目的での削除や破壊的な変更を行うことを**禁止**する。これらは決して「使い捨てのテストデータ」ではない。

## 2. 検証スクリプトの作成ガイドライン

### 2-1. スコープの限定
検証データの作成・削除を行う際は、必ず自身が作成した特定のリソース（例: 特定のプレフィックスを持つデータ、変数に保持したID）のみを対象とする。
```ruby
# GOOD
test_unit = Unit.create!(...)
# ... validation ...
test_unit.destroy!

# BAD
Unit.create!(...)
# ... validation ...
Unit.destroy_all # 絶対に禁止
```

### 2-2. トランザクションの活用
一時的なデータ検証には、トランザクションとロールバックを活用する。
```ruby
ActiveRecord::Base.transaction do
  # ... create & test ...
  raise ActiveRecord::Rollback
end
```

## 3. ユーザーへの確認
破壊的な変更を伴う可能性がある操作（DBリセット、大量データの削除など）を提案・実行する際は、必ず事前にユーザーへリスクを説明し、明示的な許可を得なければならない。
