<%= render PersonCardComponent.new(person: @person) do %>
  <div class="mt-2 pl-3 text-xs text-slate-500 dark:text-slate-400">
    <span class="font-semibold">L'Arc~en~Ciel</span>
    <span class="text-xs opacity-80">(Vocal)</span>
    <span class="mx-1">、</span>
    <span class="text-slate-400 dark:text-slate-500">VAMPS</span>
    <span class="text-xs opacity-80">(Vocal)</span>
  </div>
<% end %>
