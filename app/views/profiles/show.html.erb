<div class="flex justify-center items-center min-h-screen p-0 md:p-8">
  <div class="bg-white dark:bg-slate-800 shadow-2xl rounded-none md:rounded-3xl overflow-hidden w-full max-w-5xl animate-in slide-in-from-bottom-5 duration-700">
    <%= render ProfileHeaderComponent.new(resource: @resource) %>

    <div class="p-5 md:p-8">
      <div class="grid gap-8 grid-cols-1 md:grid-cols-[350px_1fr] items-start">
        <div>
          <%= render BasicAttributesComponent.new(resource: @resource) %>
          <%= render LinksComponent.new(links: @links) %>
          <%= render SpotifyEmbedComponent.new(links: @links) %>
        </div>

        <div class="column-right">
          <% if @resource.is_a?(Unit) %>
            <%= render UnitMembersComponent.new(
              unit: @resource,
              active_members: @active_members,
              past_members: @past_members
            ) %>
          <% end %>

          <% if (@resource.is_a?(Person) && (@logs.present? || @old_history_items.present?)) || (@resource.is_a?(Unit) && @history.present?) %>
            <section>
              <div class="flex items-center justify-between mb-6 border-b border-slate-200 dark:border-slate-700 pb-2">
                <h2 class="text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400">History</h2>
                <% if @resource.is_a?(Person) && defined?(logged_in?) && logged_in? %>
                  <%= link_to "Edit", admin_person_person_logs_path(@resource), class: "text-xs font-medium text-slate-400 hover:text-indigo-500 transition-colors" %>
                <% elsif @resource.is_a?(Unit) && defined?(logged_in?) && logged_in? %>
                  <%= link_to "Edit", admin_unit_unit_logs_path(@resource), class: "text-xs font-medium text-slate-400 hover:text-indigo-500 transition-colors" %>
                <% end %>
              </div>
              <div class="grid gap-6 relative before:absolute before:left-3 before:top-0 before:bottom-0 before:w-0.5 before:bg-slate-200 dark:before:bg-slate-700">
                <% if @resource.is_a?(Person) %>
                  <% if @logs.present? %>
                    <%= render PersonHistoryItemComponent.with_collection(@logs) %>
                  <% elsif @old_history_items.present? %>
                    <% @old_history_items.each do |item| %>
                      <div class="bg-slate-50 dark:bg-slate-900/50 rounded-2xl p-5 pl-10 relative border border-slate-100 dark:border-slate-700">
                        <div class="absolute left-[0.45rem] top-6 w-2.5 h-2.5 bg-person border-2 border-white dark:border-slate-800 rounded-full z-10 shadow-[0_0_0_2px_rgba(226,232,240,1)] dark:shadow-[0_0_0_2px_rgba(51,65,85,1)]"></div>
                        <div>
                          <h3 class="text-sm font-bold text-slate-800 dark:text-slate-200">
                            <%= link_to item[:unit_name], "/#{item[:old_key]}.html", class: "hover:text-unit transition-colors" %>
                            <% if item[:part_and_name].present? %>
                              <span class="text-xs font-bold text-slate-600 dark:text-slate-300 ml-1">(<%= item[:part_and_name] %>)</span>
                            <% end %>
                          </h3>
                        </div>
                      </div>
                    <% end %>
                  <% end %>
                <% elsif @resource.is_a?(Unit) %>
                  <% @history.group_by { |log| log.log_date.to_s.gsub("/", "-") }.each do |date, logs| %>
                    <%= render UnitHistoryGroupComponent.new(date: date, logs: logs) %>
                  <% end %>
                <% end %>
              </div>
            </section>
          <% end %>
        </div>
      </div>

      <%= render ManagementInformationComponent.new(resource: @resource) %>

      <footer class="mt-12 pt-8 border-t border-slate-100 dark:border-slate-700 text-center">
        <p class="text-[0.65rem] font-bold text-slate-400 dark:text-slate-500 uppercase tracking-widest leading-loose">
          Last updated<br>
          <span class="text-slate-300 dark:text-slate-600 font-black"><%= @resource.updated_at.strftime("%Y-%m-%d %H:%M:%S") %></span>
        </p>
      </footer>
    </div>
  </div>
</div>
